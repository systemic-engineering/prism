[package]
name = "prism"
version = "0.1.0"
edition = "2021"
description = "fold | prism | traversal | lens | iso. The five optic operations. Zero dependencies."

[dependencies]
# None. Depends on nothing. The types outlive everything around them.
